<!DOCTYPE html>
<html>
  <head>
    <title>Co.</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <ul class="inner">
        <li>Top</li>
        <% if logged_in? %>
          <li>Schedule</li>
          <li><%= link_to "Logout", logout_path, method: :delete, class: "link" %></li>
          <li><%= link_to "SignUp", new_company_path, class: "link" %></li>
          <li><%= current_member.name %></li>
        <% else %>
          <li><%= link_to "SignUp", new_company_path, class: "link" %></li>
          <li><%= link_to "Login", login_path, class: "link" %></li>
        <% end %>
      </ul>
    </header>
    <%= yield %>
  </body>
</html>
